<?php
/**
 * Файл первичной инициализации '.apprc'  -формат файла PHP
 * Его не надо подключать, это происходит автоматически, мало того
 * повторное подлючения надо избегать.
 * 
 * Подключения происходит при существованиии файла.
 * В нем прописаны сустемные константы конфигураций, индивидуальные для сервера
 */ 


/**
 * Директория всей этой поеботины
 */ 
define('SYSPATH', realpath( __DIR__) );

/**
 * Директория исходников Application
 */
define('SRC_DIR',    SYSPATH . '/app' );

/**
 * DOCUMENT_ROOT / или папочка веб фалов
 */
define('HTML_DIR',   SYSPATH . '/web' );

// Там вроде понятно
define('LOGS_DIR',   SYSPATH . "/app/logs");
define('CACHE_DIR',  SYSPATH . "/app/cache");
define('CONFIG_DIR', SYSPATH . "/app/config");
